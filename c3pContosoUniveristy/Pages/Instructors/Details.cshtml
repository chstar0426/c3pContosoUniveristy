@page
@model c3pContosoUniveristy.Pages.Instructors.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Instructor</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Instructor.LastName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Instructor.LastName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Instructor.FirstMidName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Instructor.FirstMidName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Instructor.HireDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Instructor.HireDate)
        </dd>
    </dl>
</div>


<div class="btn-group" role="group" aria-label="Basic example">
    
    <a asp-page="./Edit" asp-route-id="@Model.Instructor.ID" asp-route-path="@(Request.Query["Path"].ToString())" class="btn btn-outline-primary">수정</a>
    <a asp-page="./Delete" asp-route-id="@Model.Instructor.ID" asp-route-path="@(Request.Query["Path"].ToString())" class="btn btn-outline-danger">삭제</a>

    <a href='./Index@(Request.Query["Path"].ToString())' class="btn btn-outline-secondary">목록</a>


</div>

